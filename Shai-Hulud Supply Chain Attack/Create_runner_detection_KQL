//For command execution of GitHub Actions Runner Creation
DeviceProcessEvents
| where Timestamp > ago(30d)
| where ProcessCommandLine has_all ("config.sh","SHA1HULUD") 
  or ProcessCommandLine has_all ("config.cmd","SHA1HULUD")
| where ProcessCommandLine has "--name"
